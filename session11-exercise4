from sklearn.cluster import KMeans
import pandas as pd

df = pd.DataFrame({
    "Income": [15, 20, 60, 80, 100],
    "Spending": [10, 30, 70, 90, 95]
})

kmeans = KMeans(n_clusters=2, random_state=0)
df["Cluster"] = kmeans.fit_predict(df)

print(df)
